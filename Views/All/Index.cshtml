@model IEnumerable<CarLib.Models.Brand>

@{ 
    ViewData["Title"] = "Группировка";
}

<h1>Группировка</h1>

<div class="container">
    @foreach(var b in Model)
    {
        <div class="row mb-5">
            <h3 class="col text-center col-2 col-sm-2">@Html.DisplayFor(model => b.Name)</h3>
            <div class="col col-10 col-sm-10 justify-content-end">
                @foreach (var m in b.Models)
                {
                    <div class="col-sm-8 border text-right justify-content-end">
                        <h3 style="@(m.Active?"color:black;":"color:gray")">@Html.DisplayFor(model => m.Name)</h3>
                    </div>
                    }
                </div>
        </div>
    }
</div>